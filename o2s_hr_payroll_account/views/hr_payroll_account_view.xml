<?xml version="1.0"?>
<openerp>
    <data>

        <record model="ir.ui.view" id="view_hr_payslip_inherit_add_move_form">
            <field name="name">hr.payslip.inherit.add.move.form</field>
            <field name="model">hr.payslip</field>
            <field name="inherit_id" ref="hr_payroll_account.view_hr_payslip_inherit_form"/>
            <field name="arch" type="xml">
                <field name="move_id" position="after">
                    <field name="move_provition_id" readonly="1"/>
                </field>
            </field>
        </record>

        <record model="ir.ui.view" id="view_hr_salary_rule_add_acc_form">
            <field name="name">hr.salary.rule.add.acc.form</field>
            <field name="model">hr.salary.rule</field>
            <field name="inherit_id" ref="hr_payroll_account.hr_salary_rule_form_inherit"/>
            <field name="arch" type="xml">
                <field name="account_debit" position="attributes">
                    <attribute name="invisible">1</attribute>
                </field>
                <field name="account_credit" position="attributes">
                    <attribute name="invisible">1</attribute>
                </field>
                <page string="Accounting" position="inside">
                    <separator string="Configuración de Cuentas Contables por Unidad de Negocio"/>
                    <field name="rule_account_ids" nolabel="1">
                    	<tree editable="top">
                    		<field name="debit_account_id" domain="[('type','!=','view'),('company_id','=',parent.company_id)]"/>
                    		<field name="credit_account_id" domain="[('type','!=','view'),('company_id','=',parent.company_id)]"/>
                    		<field name="business_unit_id"/>
                    	</tree>
                    </field>
                </page>
            </field>
        </record>

    </data>
</openerp>
